{
  "name": "dot-vscode",
  "displayName": "DOT (Graphviz) Preview & Syntax",
  "description": "Syntax highlighting and live SVG preview for Graphviz DOT (.dot / .gv) files — powered by the pure-TypeScript graphviz-ts engine.",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "publisher": "knowvah",
  "categories": [
    "Programming Languages",
    "Visualization"
  ],
  "keywords": [
    "graphviz",
    "dot",
    "gv",
    "diagram",
    "preview",
    "graphviz-ts"
  ],
  "homepage": "https://github.com/knowvah/dot-plugins/tree/main/packages/vscode#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/knowvah/dot-plugins.git",
    "directory": "packages/vscode"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "main": "./dist/extension.js",
  "contributes": {
    "configuration": {
      "title": "DOT (Graphviz)",
      "properties": {
        "dot.preview.renderTimeoutSeconds": {
          "type": "number",
          "default": 60,
          "minimum": 1,
          "markdownDescription": "Maximum time (in seconds) to render a `.dot`/`.gv` file preview before treating the graph as non-terminating and aborting to a timeout message. Rendering runs in a worker thread, so this never freezes the editor — raise it for very large graphs."
        }
      }
    },
    "markdown.markdownItPlugins": true,
    "markdown.previewStyles": [
      "./media/dot-preview.css"
    ],
    "languages": [
      {
        "id": "dot",
        "aliases": [
          "Graphviz (DOT)",
          "dot",
          "graphviz"
        ],
        "extensions": [
          ".dot",
          ".gv"
        ],
        "configuration": "./language-configuration.json"
      }
    ],
    "grammars": [
      {
        "language": "dot",
        "scopeName": "source.dot",
        "path": "./syntaxes/dot.tmLanguage.json"
      }
    ],
    "commands": [
      {
        "command": "dot.showPreview",
        "title": "Open Preview to the Side",
        "category": "DOT",
        "icon": "$(open-preview)"
      }
    ],
    "menus": {
      "editor/title": [
        {
          "when": "resourceLangId == dot",
          "command": "dot.showPreview",
          "group": "navigation"
        }
      ],
      "commandPalette": [
        {
          "command": "dot.showPreview",
          "when": "resourceLangId == dot"
        }
      ]
    },
    "keybindings": [
      {
        "command": "dot.showPreview",
        "key": "ctrl+k v",
        "mac": "cmd+k v",
        "when": "resourceLangId == dot && editorTextFocus"
      }
    ]
  },
  "scripts": {
    "build": "node esbuild.config.mjs",
    "dev": "node esbuild.config.mjs --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "package": "vsce package --no-dependencies"
  },
  "devDependencies": {
    "@knowvah/dot-core": "workspace:*",
    "@knowvah/dot-markdown-it": "workspace:*",
    "@types/markdown-it": "^14.0.0",
    "@types/node": "^22.0.0",
    "@types/vscode": "^1.85.0",
    "@vscode/vsce": "^3.0.0",
    "esbuild": "^0.24.0",
    "graphviz-ts": "^0.1.0",
    "markdown-it": "^14.0.0",
    "typescript": "^6.0.3",
    "vitest": "^3.0.0"
  }
}
